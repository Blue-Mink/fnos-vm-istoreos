#!/bin/bash
# iStoreOS FPK - 更新后回调
# iStoreOS 系统版本升级需手动操作（换镜像 = 换系统，不做自动覆盖）

echo "iStoreOS 系统版本升级需要手动操作："
echo "1. 在「虚拟机」应用中停止并删除旧虚拟机（可选保留数据）"
echo "2. 卸载当前应用后重新安装并选择新版本镜像"
echo ""
echo "注意：如需保留 iStoreOS 内的配置，请先在其管理界面导出备份。"
exit 0
